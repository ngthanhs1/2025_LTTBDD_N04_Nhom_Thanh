import 'package:flutter/material.dart';

const double kPadding = 16.0;
const double kRadius = 16.0;

final kColorScheme = ColorScheme.fromSeed(
  seedColor: const Color.fromARGB(255, 214, 217, 231),
);
